target-version = "py311"
line-length = 100

[lint]
# Стартовый набор: дефолтные правила ruff (Pyflakes + часть pycodestyle-errors).
# Расширять по мере необходимости, а не всё сразу — чтобы каждое включённое
# правило разбиралось с уже существующими находками, а не копило игнор-лист.
select = ["E4", "E7", "E9", "F"]
